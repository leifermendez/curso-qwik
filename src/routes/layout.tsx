import { component$, Slot } from '@builder.io/qwik';

/**
 * Este es el archivo
 * para colocar
 * HEADER
 * Footer
 * y todo los componentes
 * que siempe van a estar presentes
 */
export default component$(() => {
  return <Slot />;
});
